// Placeholder for S-04-01: Màn hình quản lý danh sách Idle Resources
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/Card";

const ResourceListPage = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Idle Resource Management</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-text-secondary">Resource list table will be implemented here.</p>
      </CardContent>
    </Card>
  );
};

export default ResourceListPage;
